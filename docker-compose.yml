services:
  db:
    image: mysql:8.0
    container_name: mysql_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: file_manager
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin
    volumes:
      - db_data:/var/lib/mysql
    ports:
      - "3307:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 5s
      retries: 10
      start_period: 15s
      timeout: 5s

  backend:
    build: ./backend
    container_name: backend_app
    restart: always
    depends_on:
      db:
        condition: service_healthy 
    environment:
      DATABASE_URL: mysql+mysqlconnector://admin:admin@db:3306/file_manager
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    container_name: frontend_app
    restart: always
    depends_on:
      - backend
    environment:
      BACKEND_URL: http://backend:8000
    ports:
      - "5001:5001"
    volumes:
      - ./frontend:/app

volumes:
  db_data:
